{
  "name": "ktc-srv-temp-bot",
  "version": "1.0.0",
  "description": "MAX messenger bot: monitors temperature sensor API and sends alerts",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "keywords": ["max", "messenger", "bot", "temperature", "monitoring"],
  "license": "MIT",
  "engines": {
    "node": ">=18.18.0"
  },
  "dependencies": {
    "@maxhub/max-bot-api": "^0.2.2",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  }
}
